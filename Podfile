# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Tor-Connect' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Tor-Connect
  pod 'Tor'

  target 'Tor-ConnectTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Tor-ConnectUITests' do
    # Pods for testing
  end

end
